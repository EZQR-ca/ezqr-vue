name: Publish package to GitHub Packages
on:
  push:
    branches: [master]
    paths: ['package.json', 'src/index.js', 'README.md']
jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          registry-url: 'https://npm.pkg.github.com'
      - run: npm pkg set name="@ezqr-ca/ezqr-vue"
      - run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
